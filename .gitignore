# Entorno Virtual de Python
venv/
*.venv
backend_api/.venv/

# Archivos compilados de Python
backend_api/__pycache__/
backend_api/*.pyc
backend_api/*.pyo
backend_api/*.pyd
backend_api/.env # ¡MUY IMPORTANTE ignorar el archivo .env local!

# --- Node / Frontend (frontend_app) ---
frontend_app/node_modules/
frontend_app/dist/
frontend_app/.env* # Ignorar archivos .env locales del frontend también
frontend_app/.DS_Store
frontend_app/npm-debug.log*
frontend_app/yarn-debug.log*
frontend_app/yarn-error.log*


# Archivos de configuración específicos del editor/SO
.vscode/
.idea/
.DS_Store
Thumbs.db

# Archivos de dependencias (si usaras manejadores como pipenv o poetry)
# Pipfile.lock
# poetry.lock

# Archivos sensibles (ejemplo - ¡añade otros si los tienes!)
.env

# Opcional: Si no quieres subir el JSON generado cada vez (si es muy grande o temporal)
# doctores_output.json

# Opcional: Si no quieres subir el Excel original
*.xlsx
*.xlsx 
